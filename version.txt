
版本修改说明：

20181126
1、新增安装方式：将环境变量部署在用户目录下；
2、替换授权so，增加strip处理，去除调试信息；

20181031
1、修改sftp、scp命令问题；
2、增加release为4，issue为5的处理方式；
3、解决系统中不存在netstat、ifocnfig、awk命令时的问题；
4、去除启动业务时所采用的nohup命令，采用>/dev/null 2>&1;

20180621
1、增加守护进程以保证程序的稳定运行；
2、增加主板温度、风扇状态、电源故障等功能；
3、解决主机通过hostname访问的问题；
4、增加csh下的脚本处理；
5、增加卸载脚本，一键恢复系统；
6、新增并口状态告警功能；
7、将脚本中SHLVL的值改为可配置

20171030
1、解决关键目录权限变更原有权限获取的问题；  
2、增加TCP和UDP流量上报的功能；  
3、僵尸进程获取增加代码保护，修改上报格式错误
4、本地登录不处理SSH跳转链路信息
5、修改硬盘未使用率为硬盘使用率上报
6、光驱弹出告警应发送24 1 报文
7、更新linux agent版权控制和打包发布！
8、install脚本增加自启动配置

20170817
1、解决popen函数close问题
2、CPU负载采集间隔为1秒
3、USB禁用存储检测告警上报
4、解决操作命令按回车重复上报的问题
5、增加USB禁用主机插入拔出子类型37 38

20170803
1、修改bashrc脚本，在su -模式下可以上报操作回显
2、非root用户ssh登录pid获取不正确，导致操作链路被删  
3、解决定时清除文件的代码故障
4、在install.sh中创建/tmp/.record目录及其子目录，并设置相应权限

20170727
1、将配置项整合到agent.conf中，支持配置本机IP地址
2、支持license功能，默认试用1年
3、支持非法外联功能
4、支持登录次数告警
5、修正基线核查功能的错误
6、增加采集主板温度和风扇转速的功能

20170724
1、修改阻断链路操作，发送响应报文，代码处理错误。
2、修改获取USB信息，内存泄露故障。